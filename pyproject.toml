[project]
name = "pii-detection-redaction-pipeline"
version = "1.0.0"
description = "Detección, redacción y revisión humana de PII en documentos de negocio (ES/EN)"
requires-python = ">=3.11,<3.13"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
