# Runtime dependencies: everything the pipeline and the Streamlit demo need.
# Pinned from the verified environment (Python 3.11.13, macOS arm64).
#
# Install:  uv venv --python 3.11 .venv && uv pip install -r requirements.txt
# For training, evaluation and notebooks also install requirements-dev.txt.

# --- inference ---
torch==2.14.0
transformers==5.17.0
huggingface-hub==1.32.0

# --- rule engine and statistical NER ---
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
spacy==3.8.16

# spaCy models must be declared here, not installed with `spacy download`:
# hosting platforms only run `pip install -r requirements.txt`. Without them
# Presidio tries to download a model at request time and the request hangs.
# The small models keep the hosted demo inside its memory budget; a local
# `make setup` additionally installs the more accurate `md` ones and the
# detector prefers whichever is present.
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
es_core_news_sm @ https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.8.0/es_core_news_sm-3.8.0-py3-none-any.whl

# --- scoring, data handling and UI ---
scikit-learn==1.9.1
pandas==3.0.6
numpy==2.4.6
streamlit==1.64.0
